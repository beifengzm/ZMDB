#include "Database.h"
#include <stdio.h>

int main()
{
    // struct Database* pDB = newDB();
    // char str[101] = " set  aaa   aaa1   aBDavFdvF  vdGGDda     ";
    // pDB->execCommond(pDB, str);
    // for (int i = 0; i < pDB->argc; ++i)
    // {
    //     printf("argv%d: %s ", i+1, pDB->argv[i]);
    // }
    // printf("\n");

    return 0;
}